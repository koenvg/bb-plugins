# Installed quota feasibility — passed (host RPC only)

- BB 0.43.4, Plugin SDK 0.5.9, Pi runtime/AI packages 0.87.1; selected enrolled host `host_dt6w76k4w8` was connected. Built-in Provider usage remained disabled.
- Earlier failure: BB's self-contained host bundle could not resolve Pi AI's bundler-opaque Codex OAuth import. npm installs Pi AI in two module instances; importing its static loader alone registered the wrong instance. The host now registers the static loader and the public Codex provider from the **same** Pi AI instance on this plugin's private `ModelRuntime`. It does not modify Pi's global provider configuration.
- `npm test` passed (7 tests), `npm run typecheck`, `bb plugin types --check`, and `npm run test:bundle` passed. The bundle tests cover both fresh and expiring synthetic credentials: the latter stubs Pi's refresh endpoint, checks one refresh POST and the rotated credential in temporary storage, then verifies the quota GET. Neither test uses real network or user credentials. The installed plugin log was empty at inspection.
- Removed the unused scaffold `components/`, `hooks/`, `lib/`, and UI runtime dependencies. The remaining BB-shimmed type-only dev dependencies are required by `bb plugin types --check`, even though this minimal app does not import them yet.
- Installed `ping` returned `{ "reachable": true }`. Installed `probe` returned `{ "status": "verified", "generalWindows": 1, "bankedResetReported": true }` from the selected host's real Pi OAuth and Codex quota response. Only a field-presence flag was returned for banked resets; neither the count nor the window percentage was exposed by the feasibility probe.
- The one-off probe used a bounded quota GET and returned only fixed status codes, a bounded window count, and a boolean. It exposed no token, authentication claim ID, request header, or raw upstream body. After feasibility testing, its production RPC was removed; normal quota reads now use the selected-host guard, account cache, and cancellation path. No Pi model turn was sent.
- Project `proj_5zvfxt6vz4` is OpenForge; its Codex Usage plugin in the sibling `openforge-plugins` checkout uses the same Pi authentication path but externalizes the Pi package from its Vite backend. BB's host bundle is self-contained, so this plugin needs the explicit static-loader/private-runtime-provider pairing instead.
- OpenSpec task 1.2 was completed by this feasibility probe. The later normalization, account cache, sidebar/dashboard, and installed UI acceptance are documented in [ACCEPTANCE.md](ACCEPTANCE.md); this note remains the earlier, host-RPC-only checkpoint.
